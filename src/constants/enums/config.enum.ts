export enum ConfigKeyEnum {
  Database = 'database',
  App = 'app',
  Cors = 'cors',
  Cloudinary = 'cloudinary',
  Crypto = 'crypto',
}

export enum AppConfig {
  PORT = 'PORT',
  NODE_ENV = 'NODE_ENV',
}
